---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web-dev
  namespace: awesomehytale-com-dev
spec:
  entryPoints:
    - websecure
  routes:
    # Route for Authentik outpost callbacks - required for auth flow
    - kind: Rule
      match: Host(`dev.awesomehytale.com`) && PathPrefix(`/outpost.goauthentik.io/`)
      services:
        - kind: Service
          name: authentik-server
          namespace: authentik
          port: 80
    # Main route with forwardAuth
    - kind: Rule
      match: Host(`dev.awesomehytale.com`)
      services:
        - name: web
          port: 80
      middlewares:
        - name: authentik-forwardauth
  tls:
    secretName: awesomehytale-com-dev-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web-dev-http
  namespace: awesomehytale-com-dev
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`dev.awesomehytale.com`)
      services:
        - name: web
          port: 80
      middlewares:
        - name: https-redirect
          namespace: awesomehytale-com-dev
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: https-redirect
  namespace: awesomehytale-com-dev
spec:
  redirectScheme:
    scheme: https
    permanent: true
