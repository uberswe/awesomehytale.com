apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: awesomehytale-com-prod
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
